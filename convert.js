function isEqual()
	{
	 let a = [1, 2, 4, 5];
	  // Comparing each element of array
	   for(let i=1;i<a.length;i++)
	   if([0] + [1] !== [2] ){
		return " Not Equal";
	   }else{
		return "Equal";
		}
	  }
	
console.log(isEqual());